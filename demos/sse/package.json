{
  "name": "sse-demo",
  "private": true,
  "type": "module",
  "dependencies": {
    "@remix-run/fetch-router": "workspace:*",
    "@remix-run/session": "workspace:*",
    "@remix-run/logger-middleware": "workspace:*",
    "@remix-run/node-fetch-server": "workspace:*",
    "@remix-run/sse": "workspace:*",
    "@remix-run/html-template": "workspace:*",
    "@remix-run/interaction": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "esbuild": "^0.25.10",
    "tsx": "^4.20.6"
  },
  "scripts": {
    "dev": "NODE_ENV=development tsx watch server.ts",
    "start": "tsx server.ts",
    "test": "NODE_ENV=test NODE_OPTIONS=--expose-gc tsx --test",
    "typecheck": "tsc --noEmit"
  }
}
